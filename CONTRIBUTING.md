# Contributing

- Branch naming: feature/short-description (e.g., feature/login-page)
- All code changes go through pull requests into master — no direct pushes
- Every PR needs 1 approval from another team member before merging
- CI (GitHub Actions) must pass before merge
